#pragma once
#include <cstdint>

namespace Configuration
{
	using FloatType = float;

	const uint32_t MAX_FRAMERATE = 1000000u;

	const sf::Vector2u WINDOW_SIZE = sf::Vector2u(1920u, 1080u);

	const uint32_t PIXEL_SIZE = 3u;
};

